# 스프링 빈과 의존관계

## 컴포넌트 스캔과 자동 의존관계 설정

- 컨트롤러가 멤버 서비스를 의존한다.

### @Controller

- spring container 이 @Controller Annotation을 보고 MemberController 라는 객체를 생성, 스프링이 관리함

### @Autowired

- 생성자에 Autowired 라고 되어있으면 스프링이 MemberService 를 spring container 에 있는 MemberService 에 연결을 해줌

### 내부 동작 원리

1. 스프링 컨테이너와 빈 등록

- `@Component`, `@Service`, `@Repository`, `@Controller` 같은 애노테이션이 붙은 클래스들이 스프링 컨테이너에 빈으로 등록됨

2. BeanPostProcessor 의 역할

- 스프링은 `@Autowired`, `@Inject`, `@Resource` 같은 애노테이션을 처리하기 위해 `AutowiredAnnotationBeanPostProcessor` 라는 특별한 `BeanPostProcessor` 를 사용

- BeanPostProcessor 는 빈이 생성된 직후, 초기화 단계에서 해당 클래스의 필드/생성자/메서드를 스캔

3. 리플렉션으로 의존성 탐색

- 예를 들어, MemberController 의 생성자를 보면 MemberService 타입 파라미터가 필요하다. BeanPostProcessor 는 리플렉션을 통해 "이 생성자에 어떤 타입이 필요한가?" 를 확인한다. 컨테이너 안에 등록된 빈 목록을 뒤져서 MemberService 타입과 호환되는 빈을 찾는다.

4. 매칭 규칙

- 기본적으로 타입 매칭을 먼저 실행
  - MemberService 타입 빈이 하나만 있으면 바로 주입
  - 여러 개 있으면 `@Qualifier` 나 빈 이름을 기준으로 추가 매칭

5. 객체 생성 및 주입

- 스프링은 `new MemberController(memberService)` 처럼 실제 객체를 생성하면서, 찾은 `MemberService` 빈을 인자로 전달
- 결과적으로 MemberController 는 MemberService 와 연결된 상태로 컨테이너에 등록됨

📌 흐름 요약

1. 컨테이너가 모든 빈 등록
2. BeanPostProcessor 가 `@Autowired` 붙은 생성자/필드 스캔
3. 리플렉션으로 필요한 타입 확인
4. 컨테이너에서 해당 타입의 빈을 찾음
5. 객체 생성 시 그 빈을 인자로 넣어줌

👉 결국, 의존성 주입은 객체 생성 시 필요한 다른 객체를 스프링이 넣어주는 것 !

- 스프링은 스프링 컨테이너에 스프링 빈을 등록할 때, 기본으로 싱글톤으로 등록한다.(유일하게 하나만 등록해서 공유한다.)